# Quorum — environment variables
# Copy this file to .env and fill in real values. Never commit .env.

# --- Milestone 1: orchestrator ---
# Anthropic API key for the LangGraph supervisor and specialist agents.
ANTHROPIC_API_KEY=

# Anthropic API base URL. Leave blank for the default (https://api.anthropic.com).
# Set to your proxy / gateway endpoint if you route Claude traffic through one.
ANTHROPIC_BASE_URL=

# Optional: override the model used by every agent (default claude-sonnet-4-6).
QUORUM_MODEL=claude-sonnet-4-6

# --- Milestone 2+: Solana edge (not required for Milestone 1) ---
# Solana RPC endpoint. For devnet use https://api.devnet.solana.com
SOLANA_RPC_URL=https://api.devnet.solana.com

# Base58-encoded Solana keypair secret for the operator wallet (devnet only).
SOLANA_PRIVATE_KEY=

# Helius API key (Milestone 5, Flow agent on-chain enrichment).
HELIUS_API_KEY=
